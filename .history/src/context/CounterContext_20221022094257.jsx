import React,{createContext} from 'React'
export const counterContext=createContext(0);
const CounterContextProvider = () => {
  return (
    <div>
      
    </div>
  )
}

export default CounterContextProvid
